<h1>Forecasts</h1>

<% if @forecasts.present? %>
  <table>
    <thead>
      <tr>
        <th>Postcode</th>
        <th>Rating</th>
      </tr>
    </thead>

    <tbody>
      <% @forecasts.each do |forecast| %>
        <tr>
          <td><%= forecast.postcode %></td>
          <td><%= forecast.heat_rating %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
